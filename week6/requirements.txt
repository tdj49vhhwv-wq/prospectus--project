# PE/VC 招股说明书融资历史提取 — Week 6 主要依赖
# Python >= 3.9

PyMuPDF>=1.23.0        # PDF文本提取 (fitz)
pdfplumber>=0.10.0     # PDF表格提取（合并单元格处理）
pydantic>=2.0          # Schema校验
openpyxl>=3.1.0        # Excel输出
pg8000>=1.30.0         # PostgreSQL连接（数据库存储任务）
requests>=2.31.0       # DeepSeek API调用
pytest>=8.0.0          # 离线单元测试
paddleocr>=2.7.0       # 流程图OCR（云汉芯城p56-58，可选）
paddlepaddle>=2.5.0    # PaddleOCR后端（可选）
numpy>=1.24.0          # 数值计算
pandas>=2.0.0          # 数据处理
