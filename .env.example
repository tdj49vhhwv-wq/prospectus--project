# Copy to .env for local reference only; this project does not auto-load it.
DEEPSEEK_API_KEY=

# Optional: override the default repository path week1/review.
PROSPECTUS_MD_DIR=

# Optional database configuration for database-only scripts and tests.
DB_HOST=
DB_PORT=5433
DB_NAME=student
DB_USER=
DB_PASSWORD=
